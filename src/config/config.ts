export enum EServer {
    allHost = "0.0.0.0",
    httpPort = 4500,
    tcpPort = 45682,
    socketPort = 4700,
    httpsPort = 45683
}

export enum EDB {
    dbhost = 'localhost',
    dbname = 'chat',
    dbuser = 'root',
    dbpass = '@5martH67',
    dbdialect = 'mysql',
    dbport = '',
    timezone = '+07:00'
}

export enum EGraph {
    url = 'https://graph.facebook.com',
    version = "v12.0"
}
